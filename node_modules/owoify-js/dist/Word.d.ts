export default class Word {
    private word;
    private replacedWords;
    constructor(word: string);
    replace(searchValue: any, replaceValue: any, replaceReplacedWords?: boolean): Word;
    toString(): string;
}
